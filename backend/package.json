{
  "name": "backend",
  "type": "module",
  "scripts": {
    "dev": "nodemon server.js",
    "migrate": "prisma migrate dev --name init --schema=../prisma/schema.prisma --env-file=.env",
    "generate": "prisma generate --schema=../prisma/schema.prisma"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "express-validator": "^7.2.1",
    "jsonwebtoken": "^9.0.2",
    "@prisma/client": "^6.9.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
